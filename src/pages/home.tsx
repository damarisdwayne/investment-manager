import React from "react";
import { Box } from "@damaris-ui/core";
import { Header } from "@/components/header";

// import { Container } from './styles';

const Home: React.FC = () => {
  return (
    <Box withPadding fullWidth>
      <Header />
    </Box>
  );
};

export default Home;
